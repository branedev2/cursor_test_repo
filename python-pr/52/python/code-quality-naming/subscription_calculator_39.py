# {fact rule=code-quality-naming@v1.0 defects=0}
def calculate_monthly_subscription_fee():
    subscription_duration_months = 12
    monthly_fee_amount = 29.99
    return subscription_duration_months * monthly_fee_amount
# {/fact}